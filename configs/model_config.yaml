# Model Configuration
# Settings for training and inference models

process_model:
  isolation_forest:
    contamination: 0.1
    n_estimators: 100
  xgboost:
    max_depth: 6
    learning_rate: 0.1
    n_estimators: 100

file_model:
  catboost:
    max_depth: 6
    learning_rate: 0.1
    iterations: 100

network_model:
  isolation_forest:
    contamination: 0.1
    n_estimators: 100
  xgboost:
    max_depth: 6
    learning_rate: 0.1
    n_estimators: 100

aggregator_model:
  xgboost:
    max_depth: 6
    learning_rate: 0.1
    n_estimators: 100
